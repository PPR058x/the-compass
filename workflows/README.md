# workflows/

Multi-step pipelines that orchestrate one or more agents from `agents/` toward a larger outcome — e.g. a release pipeline that chains a lyrics agent, a branding-review agent, and a marketing agent in sequence.

Currently a scaffold — empty until the first workflow is defined. See `docs/ARCHITECTURE.md` for how this fits into the overall system.
