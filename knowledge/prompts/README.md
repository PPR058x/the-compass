# knowledge/prompts/

Reference notes about prompt conventions and patterns — what's worked, brand-consistent phrasing, generation guidelines — that inform how prompts get written.

Distinct from the top-level `prompts/` (agent system/task prompt templates, ready to run) and the existing root `Prompts/` (brand-consistent image/video/audio generation prompts for the campaign). This folder holds the *knowledge behind* those prompts, not the prompts themselves.

Currently empty.
