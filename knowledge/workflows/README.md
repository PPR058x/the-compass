# knowledge/workflows/

Reference knowledge about how studio workflows are built and run — patterns, lessons learned, common pipeline shapes — that informs the actual pipelines defined under the top-level `workflows/`.

Distinct from the top-level `workflows/`, which holds the runnable pipeline definitions themselves. This folder holds the knowledge behind designing them.

Currently empty.
