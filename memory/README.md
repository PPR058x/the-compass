# memory/

Persistent agent memory, state, and session logs — the record of what agents have decided, produced, or learned over time, so future runs have continuity instead of starting cold.

Currently a scaffold — empty until agents start writing memory. See `docs/ARCHITECTURE.md` for how this fits into the overall system.
